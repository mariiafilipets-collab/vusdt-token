# 🔄 Процесс обработки запросов на конвертацию

## 📋 Общий процесс конвертации VUSDT → USDT

### Этап 1: Пользователь создает запрос
1. Пользователь отправляет запрос на конвертацию через `requestConversion()`
2. **VUSDT токены блокируются** - переводятся в контракт `ConversionRequest`
3. Токены **заблокированы на 14 дней** - в этот период на них не начисляются проценты
4. Статус запроса: **Pending** (Ожидает обработки)

---

## 🔧 Действия администратора

### Шаг 1: "Mark as Processing" (Пометить как обрабатывается)

**Что происходит при нажатии кнопки "Mark as Processing":**

```solidity
function markAsProcessing(uint256 requestId) external onlyOwner {
    // 1. Проверяет, что запрос существует и имеет статус Pending
    // 2. Меняет статус с Pending на Processing
    // 3. Эмитирует событие RequestStatusUpdated
}
```

**Результат:**
- ✅ Статус запроса меняется: **Pending → Processing**
- ✅ В админ панели запрос отображается с новым статусом
- ⚠️ **НИЧЕГО БОЛЬШЕ НЕ ПРОИСХОДИТ!**
- ⚠️ Токены остаются заблокированными в контракте
- ⚠️ USDT НЕ отправляется автоматически

**Это просто маркер для администратора**, что запрос взят в работу.

---

### Шаг 2: Ожидание 14 дней

- После создания запроса должно пройти **14 дней** (CONVERSION_PERIOD)
- Только после истечения этого периода можно завершить конвертацию
- В админ панели отображается: "⏳ X days remaining" или "✓ Ready for processing"

---

### Шаг 3: Ручная отправка USDT (ВАЖНО!)

**⚠️ КРИТИЧЕСКИ ВАЖНО: Администратор должен ВРУЧНУЮ отправить USDT пользователю!**

**Что нужно сделать:**

1. Откройте MetaMask или другой кошелек
2. Убедитесь, что у вас есть USDT (BEP-20) на адресе `treasuryWallet`
3. Отправьте USDT пользователю:
   - **Адрес получателя:** Адрес из поля "Requester" в запросе
   - **Сумма:** Равна сумме VUSDT из запроса (1:1)
   - **Токен:** USDT (BEP-20) на BNB Chain
   - **Адрес USDT:** `0x55d398326f99059fF775485246999027B3197955`

**Пример:**
- Запрос: 1000 VUSDT
- Отправьте: 1000 USDT на адрес пользователя

---

### Шаг 4: "Complete Conversion" (Завершить конвертацию)

**Что происходит при нажатии кнопки "Complete Conversion":**

```solidity
function completeConversion(uint256 requestId) external onlyOwner {
    // 1. Проверяет, что прошло 14 дней (block.timestamp >= lockedUntil)
    // 2. Проверяет, что статус Pending или Processing
    // 3. Меняет статус на Completed
    // 4. Разблокирует баланс пользователя (lockedBalances уменьшается)
    // 5. Эмитирует событие ConversionCompleted
}
```

**Результат:**
- ✅ Статус запроса меняется: **Processing → Completed**
- ✅ `lockedBalances[requester]` уменьшается (токены больше не считаются заблокированными)
- ⚠️ **VUSDT токены остаются в контракте** (не возвращаются пользователю)
- ⚠️ **USDT НЕ отправляется автоматически** - это нужно было сделать вручную на шаге 3!

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

### 1. USDT отправляется ВРУЧНУЮ
- Контракт **НЕ отправляет USDT автоматически**
- Администратор должен **самостоятельно** отправить USDT пользователю
- Это делается через MetaMask или другой кошелек
- **ОБЯЗАТЕЛЬНО** отправьте USDT ПЕРЕД нажатием "Complete Conversion"

### 2. VUSDT остаются в контракте
- После `completeConversion()` VUSDT токены **остаются в контракте**
- Они больше не считаются заблокированными для пользователя
- В будущем можно добавить функцию для сжигания этих токенов

### 3. Порядок действий
```
1. Пользователь создает запрос → VUSDT блокируются
2. Админ: "Mark as Processing" → Статус меняется
3. Ждем 14 дней
4. Админ: ВРУЧНУЮ отправляет USDT пользователю ← ВАЖНО!
5. Админ: "Complete Conversion" → Запрос завершен
```

### 4. Проверка перед завершением
Перед нажатием "Complete Conversion" убедитесь:
- ✅ Прошло 14 дней с момента создания запроса
- ✅ Вы ВРУЧНУЮ отправили USDT пользователю
- ✅ У вас есть транзакция отправки USDT (hash транзакции для проверки)

---

## 📊 Статусы запросов

| Статус | Описание | Действия |
|--------|----------|----------|
| **Pending** | Запрос создан, ожидает обработки | Админ может: Mark as Processing, Cancel |
| **Processing** | Админ взял запрос в работу | Админ может: Complete (после 14 дней), Cancel |
| **Completed** | Конвертация завершена | Никаких действий |
| **Cancelled** | Запрос отменен | VUSDT возвращены пользователю |

---

## 🔍 Как проверить, что USDT отправлен

1. Откройте BSCScan: https://bscscan.com
2. Найдите адрес пользователя (Requester)
3. Проверьте транзакции USDT (BEP-20)
4. Убедитесь, что есть входящая транзакция USDT на нужную сумму

---

## 💡 Рекомендации

1. **Документируйте транзакции:**
   - Сохраняйте hash транзакции отправки USDT
   - Записывайте дату и сумму

2. **Проверяйте перед завершением:**
   - Убедитесь, что USDT действительно получен пользователем
   - Проверьте адрес получателя дважды

3. **Используйте "Mark as Processing":**
   - Помечайте запросы, которые уже в работе
   - Это помогает отслеживать прогресс

---

## 🚨 Что делать, если ошибка?

### Если случайно нажали "Complete Conversion" до отправки USDT:
1. Немедленно отправьте USDT пользователю вручную
2. Свяжитесь с пользователем и объясните ситуацию
3. Сохраните hash транзакции для доказательства

### Если нужно отменить запрос:
- Используйте кнопку **"Cancel"**
- VUSDT будут возвращены пользователю
- Запрос получит статус **Cancelled**

---

**Помните: Конвертация требует РУЧНОЙ отправки USDT администратором! 🎯**

