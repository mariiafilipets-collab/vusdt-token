# 🚀 Полное руководство по публикации VUSDT проекта

## 📋 Содержание

1. [Публикация фронтенда](#1-публикация-фронтенда)
2. [Деплой контрактов на BNB Chain Mainnet](#2-деплой-контрактов-на-bnb-chain-mainnet)
3. [Расчет стоимости газа](#3-расчет-стоимости-газа)
4. [Настройка после деплоя](#4-настройка-после-деплоя)
5. [Проверка и верификация](#5-проверка-и-верификация)

---

## 1. Публикация фронтенда

### 1.1 Рекомендации по выбору хостинга

#### 🥇 **Лучшие варианты (рекомендуется):**

**1. Vercel (БЕСПЛАТНО) ⭐ РЕКОМЕНДУЕТСЯ**
- ✅ Бесплатный хостинг для React приложений
- ✅ Автоматический деплой из GitHub
- ✅ SSL сертификат включен
- ✅ CDN по всему миру
- ✅ Простая настройка
- ✅ Поддержка переменных окружения
- **Ссылка:** https://vercel.com

**2. Netlify (БЕСПЛАТНО)**
- ✅ Бесплатный хостинг
- ✅ Автоматический деплой из GitHub
- ✅ SSL сертификат
- ✅ Простая настройка
- **Ссылка:** https://netlify.com

**3. GitHub Pages (БЕСПЛАТНО)**
- ✅ Бесплатный хостинг
- ✅ Интеграция с GitHub
- ⚠️ Требует настройки для SPA
- **Ссылка:** https://pages.github.com

#### 💰 **Платные варианты (для продакшена):**

**4. AWS Amplify**
- ✅ Масштабируемость
- ✅ Интеграция с AWS сервисами
- 💰 Платный (но есть бесплатный tier)
- **Ссылка:** https://aws.amazon.com/amplify

**5. Cloudflare Pages**
- ✅ Быстрый CDN
- ✅ Бесплатный tier
- ✅ Отличная производительность
- **Ссылка:** https://pages.cloudflare.com

---

### 1.2 Пошаговая инструкция: Деплой на Vercel

#### Шаг 1: Подготовка проекта

```bash
# 1. Убедитесь, что проект готов к сборке
cd D:\VUSDT\frontend

# 2. Проверьте package.json
# Убедитесь, что есть скрипт "build"
```

#### Шаг 2: Создание production build

```bash
# Соберите проект для продакшена
npm run build

# Проверьте, что папка build/ создана
# В ней должны быть оптимизированные файлы
```

#### Шаг 3: Настройка переменных окружения

Создайте файл `.env.production` в папке `frontend/`:

```env
REACT_APP_VUSDT_ADDRESS=0x... (адрес контракта на mainnet)
REACT_APP_YIELD_DISTRIBUTOR_ADDRESS=0x...
REACT_APP_PURCHASE_CONTRACT_ADDRESS=0x...
REACT_APP_CONVERSION_CONTRACT_ADDRESS=0x...
REACT_APP_USDT_TOKEN_ADDRESS=0x... (адрес USDT на BNB Chain)
```

#### Шаг 4: Загрузка на GitHub

```bash
# 1. Инициализируйте git (если еще не сделано)
cd D:\VUSDT
git init

# 2. Создайте .gitignore (если нет)
# Убедитесь, что .env файлы в .gitignore

# 3. Добавьте файлы
git add .

# 4. Создайте коммит
git commit -m "Initial commit - VUSDT project"

# 5. Создайте репозиторий на GitHub
# Перейдите на https://github.com/new
# Создайте новый репозиторий (например, vusdt-token)

# 6. Подключите удаленный репозиторий
git remote add origin https://github.com/ваш-username/vusdt-token.git

# 7. Загрузите код
git push -u origin main
```

#### Шаг 5: Деплой на Vercel

1. **Зарегистрируйтесь на Vercel:**
   - Перейдите на https://vercel.com
   - Войдите через GitHub аккаунт

2. **Импортируйте проект:**
   - Нажмите "Add New Project"
   - Выберите ваш репозиторий `vusdt-token`
   - Vercel автоматически определит React проект

3. **Настройте проект:**
   - **Framework Preset:** Create React App
   - **Root Directory:** `frontend`
   - **Build Command:** `npm run build`
   - **Output Directory:** `build`

4. **Добавьте переменные окружения:**
   - В настройках проекта → Environment Variables
   - Добавьте все переменные из `.env.production`:
     ```
     REACT_APP_VUSDT_ADDRESS
     REACT_APP_YIELD_DISTRIBUTOR_ADDRESS
     REACT_APP_PURCHASE_CONTRACT_ADDRESS
     REACT_APP_CONVERSION_CONTRACT_ADDRESS
     REACT_APP_USDT_TOKEN_ADDRESS
     ```

5. **Деплой:**
   - Нажмите "Deploy"
   - Дождитесь завершения сборки
   - Получите URL: `https://ваш-проект.vercel.app`

6. **Настройка домена (опционально):**
   - Settings → Domains
   - Добавьте свой домен (если есть)

---

### 1.3 Альтернатива: Деплой на Netlify

#### Шаг 1: Подготовка

```bash
# Соберите проект
cd D:\VUSDT\frontend
npm run build
```

#### Шаг 2: Деплой

1. **Зарегистрируйтесь:** https://netlify.com
2. **Перетащите папку `build/`** в Netlify Dashboard
3. **Или подключите GitHub:**
   - Add new site → Import from Git
   - Выберите репозиторий
   - Build command: `cd frontend && npm run build`
   - Publish directory: `frontend/build`

4. **Добавьте переменные окружения:**
   - Site settings → Environment variables
   - Добавьте все REACT_APP_* переменные

---

## 2. Деплой контрактов на BNB Chain Mainnet

### 2.1 Подготовка

#### Шаг 1: Получите BNB для газа

**Минимально необходимо:**
- **Для деплоя контрактов:** ~0.1-0.3 BNB
- **Для верификации:** ~0.01 BNB
- **Резерв на операции:** ~0.1 BNB
- **ИТОГО: ~0.5-1 BNB рекомендуется**

**Где купить BNB:**
1. Binance Exchange (https://binance.com)
2. Trust Wallet
3. MetaMask (через встроенный обмен)
4. PancakeSwap (обмен других токенов)

#### Шаг 2: Настройте .env для mainnet

Создайте/обновите файл `.env` в корне проекта:

```env
# BNB Chain Mainnet
PRIVATE_KEY=ваш_приватный_ключ_кошелька
BSCSCAN_API_KEY=ваш_api_ключ_bscscan

# Адреса на mainnet (заполните после деплоя)
VUSDT_ADDRESS=
YIELD_DISTRIBUTOR_ADDRESS=
PURCHASE_CONTRACT_ADDRESS=
CONVERSION_CONTRACT_ADDRESS=

# Настройки для покупки
CENTRAL_WALLET=адрес_вашего_кошелька
USDT_TOKEN_ADDRESS=0x55d398326f99059fF775485246999027B3197955  # USDT на BNB Chain
TREASURY_WALLET=адрес_вашего_кошелька
```

**⚠️ ВАЖНО:**
- НЕ коммитьте `.env` в Git
- Храните приватный ключ в безопасности
- Используйте отдельный кошелек для деплоя (не основной)

#### Шаг 3: Получите BSCScan API Key

1. Зарегистрируйтесь на https://bscscan.com
2. Перейдите в Account → API-KEYs
3. Создайте новый API Key
4. Скопируйте ключ в `.env`

---

### 2.2 Деплой контрактов

#### Шаг 1: Проверьте баланс

```bash
# Проверьте баланс кошелька на BNB Chain Mainnet
# В MetaMask переключитесь на BNB Chain Mainnet
# Убедитесь, что есть минимум 0.5 BNB
```

#### Шаг 2: Деплой контрактов

```bash
# Перейдите в корень проекта
cd D:\VUSDT

# Запустите деплой на mainnet
npm run deploy:mainnet
```

**Что произойдет:**
1. Компиляция контрактов
2. Деплой VUSDT токена
3. Деплой YieldDistributor
4. Деплой VUSDTPurchase
5. Деплой ConversionRequest
6. Настройка связей между контрактами
7. Сохранение адресов в `deployment-info.json`

**Примерный вывод:**
```
Deploying contracts with the account: 0x...
Account balance: 0.5 BNB

1. Deploying VUSDT Token...
VUSDT Token deployed to: 0x...

2. Deploying YieldDistributor...
YieldDistributor deployed to: 0x...

3. Configuring contracts...
...

=== Deployment Summary ===
Network: bscMainnet
VUSDT Token: 0x...
YieldDistributor: 0x...
...
```

#### Шаг 3: Сохраните адреса

После деплоя адреса будут сохранены в `deployment-info.json`.

**Обновите `.env` файл:**
```env
VUSDT_ADDRESS=0x... (из deployment-info.json)
YIELD_DISTRIBUTOR_ADDRESS=0x...
PURCHASE_CONTRACT_ADDRESS=0x...
CONVERSION_CONTRACT_ADDRESS=0x...
```

#### Шаг 4: Верификация контрактов на BSCScan

```bash
# Верификация автоматически запустится после деплоя
# Если нет, используйте:

npx hardhat verify --network bscMainnet <CONTRACT_ADDRESS> <CONSTRUCTOR_ARGS>
```

**Пример:**
```bash
# VUSDT
npx hardhat verify --network bscMainnet 0x... 0x... (адрес владельца)

# YieldDistributor
npx hardhat verify --network bscMainnet 0x... 0x... (адрес VUSDT) 0x... (адрес владельца)
```

---

### 2.3 Настройка контрактов после деплоя

#### Шаг 1: Минт начального количества токенов

```bash
# Используйте Hardhat console
npx hardhat console --network bscMainnet

# В консоли:
const vusdt = await ethers.getContractAt("VUSDT", "0x...");
await vusdt.mint("0x...", ethers.parseEther("1000000")); // 1M токенов
```

#### Шаг 2: Регистрация держателей

```bash
# Зарегистрируйте начальных держателей
const distributor = await ethers.getContractAt("YieldDistributor", "0x...");
await distributor.registerHolders(["0x...", "0x..."]);
```

#### Шаг 3: Настройка адресов в контрактах

Убедитесь, что все адреса настроены:
- VUSDT → YieldDistributor
- VUSDT → PurchaseContract
- PurchaseContract → CentralWallet
- ConversionRequest → TreasuryWallet

---

## 3. Расчет стоимости газа

### 3.1 Стоимость деплоя контрактов

**Примерные затраты газа (на декабрь 2024):**

| Контракт | Размер | Газ (примерно) | Стоимость (BNB) |
|----------|--------|----------------|-----------------|
| VUSDT | ~15 KB | ~2,500,000 | ~0.01-0.02 |
| YieldDistributor | ~25 KB | ~3,500,000 | ~0.015-0.03 |
| VUSDTPurchase | ~12 KB | ~2,000,000 | ~0.01-0.02 |
| ConversionRequest | ~18 KB | ~2,800,000 | ~0.015-0.025 |
| **ИТОГО** | | **~10,800,000** | **~0.05-0.1 BNB** |

**Дополнительные операции:**
- Настройка связей: ~0.01-0.02 BNB
- Минт токенов: ~0.01-0.02 BNB
- Регистрация держателей: ~0.005-0.01 BNB
- Верификация: бесплатно (только газ на транзакции)

**Общая стоимость деплоя: ~0.1-0.2 BNB**

### 3.2 Текущая цена газа на BNB Chain

**Проверьте текущую цену:**
- https://bscscan.com/gastracker
- https://bscscan.com/chart/gasprice

**Рекомендации:**
- Деплой в непиковое время (ночь по UTC)
- Используйте стандартную цену газа (Standard)
- Не используйте быструю (Fast) - дороже

### 3.3 Оптимизация газа

**Советы:**
1. Используйте оптимизатор в `hardhat.config.js` (уже настроен)
2. Деплойте в непиковое время
3. Используйте batch транзакции где возможно
4. Проверьте размер контрактов (максимум 24 KB)

---

## 4. Настройка после деплоя

### 4.1 Обновление фронтенда

После деплоя контрактов обновите переменные окружения на хостинге:

**Vercel:**
1. Settings → Environment Variables
2. Обновите все `REACT_APP_*` переменные с адресами mainnet
3. Передеплойте проект

**Netlify:**
1. Site settings → Environment variables
2. Обновите переменные
3. Trigger deploy

### 4.2 Настройка автоматической дистрибуции

#### Вариант 1: Cron Job (рекомендуется)

**Используйте сервис для автоматического запуска:**

1. **Cron-job.org** (бесплатно):
   - Зарегистрируйтесь на https://cron-job.org
   - Создайте задачу:
     - URL: `https://ваш-api.com/distribute-yield`
     - Schedule: Каждую пятницу в 00:00 UTC
     - Method: POST

2. **EasyCron** (бесплатный tier):
   - https://www.easycron.com

3. **GitHub Actions** (бесплатно):
   - Создайте `.github/workflows/distribute-yield.yml`
   - Настройте запуск каждую пятницу

#### Вариант 2: VPS сервер

Если у вас есть VPS:
```bash
# Установите cron
crontab -e

# Добавьте строку (каждую пятницу в 00:00 UTC)
0 0 * * 5 cd /path/to/VUSDT && npm run distribute -- --network bscMainnet
```

### 4.3 Мониторинг

**Настройте мониторинг:**
1. BSCScan Alerts (бесплатно)
2. Telegram бот для уведомлений
3. Email уведомления о важных транзакциях

---

## 5. Проверка и верификация

### 5.1 Проверка контрактов

1. **Откройте BSCScan:**
   - https://bscscan.com/address/0x... (адрес контракта)

2. **Проверьте:**
   - ✅ Контракт верифицирован
   - ✅ Код доступен для просмотра
   - ✅ Транзакции видны

3. **Проверьте связи:**
   - VUSDT → YieldDistributor настроен
   - VUSDT → PurchaseContract настроен
   - Все адреса корректны

### 5.2 Тестирование функций

**Проверьте:**
1. ✅ Покупка VUSDT за USDT
2. ✅ Регистрация держателей
3. ✅ Дистрибуция процентов
4. ✅ Конвертация VUSDT → USDT
5. ✅ Админ панель работает

### 5.3 Безопасность

**Проверьте:**
- ✅ Приватный ключ не в Git
- ✅ .env файлы в .gitignore
- ✅ Используется отдельный кошелек для деплоя
- ✅ Мультисиг для важных операций (опционально)

---

## 📊 Итоговая стоимость

### Минимальные затраты:

| Операция | Стоимость (BNB) |
|----------|-----------------|
| Деплой контрактов | 0.1-0.2 |
| Настройка и минт | 0.02-0.03 |
| Резерв на операции | 0.1-0.2 |
| **ИТОГО** | **~0.3-0.5 BNB** |

### Рекомендуемый резерв:

**~1 BNB** для комфортной работы:
- Деплой: 0.2 BNB
- Операции: 0.3 BNB
- Резерв: 0.5 BNB

---

## 🎯 Чеклист перед запуском

- [ ] Получен BNB для газа (минимум 0.5 BNB)
- [ ] Настроен `.env` с приватным ключом
- [ ] Получен BSCScan API Key
- [ ] Контракты задеплоены на mainnet
- [ ] Контракты верифицированы на BSCScan
- [ ] Адреса контрактов обновлены в фронтенде
- [ ] Фронтенд задеплоен на хостинг
- [ ] Переменные окружения настроены на хостинге
- [ ] Протестированы все функции
- [ ] Настроена автоматическая дистрибуция
- [ ] Настроен мониторинг

---

## 🆘 Поддержка и помощь

**Полезные ссылки:**
- BNB Chain Docs: https://docs.bnbchain.org
- BSCScan: https://bscscan.com
- Hardhat Docs: https://hardhat.org/docs
- Vercel Docs: https://vercel.com/docs

**Если возникли проблемы:**
1. Проверьте баланс BNB
2. Проверьте правильность адресов
3. Проверьте логи транзакций на BSCScan
4. Убедитесь, что используете mainnet, а не testnet

---

**Готово! Ваш проект готов к запуску в продакшене! 🚀**

