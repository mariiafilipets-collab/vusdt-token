# 📤 Загрузка проекта на GitHub и деплой на Vercel

## Шаг 1: Подготовка проекта

### 1.1 Проверка .gitignore

Убедитесь, что `.gitignore` содержит все чувствительные файлы:
- ✅ `.env` файлы
- ✅ `node_modules/`
- ✅ `frontend/.env`
- ✅ `deployment-info.json` (опционально, если содержит приватные данные)

### 1.2 Создание README (если нужно)

Убедитесь, что есть `README.md` с описанием проекта.

---

## Шаг 2: Загрузка на GitHub

### 2.1 Создание первого коммита

```bash
# Перейдите в корень проекта
cd D:\VUSDT

# Проверьте статус
git status

# Создайте первый коммит
git commit -m "Initial commit - VUSDT Token with weekly yield distribution"

# Или если нужно добавить все файлы
git add .
git commit -m "Initial commit - VUSDT Token with weekly yield distribution"
```

### 2.2 Подключение к GitHub репозиторию

```bash
# Добавьте remote репозиторий
git remote add origin https://github.com/mariiafilipets-collab/vusdt-token.git

# Или если репозиторий уже существует с другим именем:
# git remote add origin https://github.com/mariiafilipets-collab/название-репозитория.git

# Проверьте подключение
git remote -v
```

### 2.3 Создание репозитория на GitHub (если еще не создан)

1. Перейдите на https://github.com/mariiafilipets-collab
2. Нажмите "New repository" (или "+" → "New repository")
3. Название: `vusdt-token` (или любое другое)
4. Описание: "VUSDT Token with weekly yield distribution on BNB Chain"
5. Выберите: **Public** (или Private, если хотите)
6. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
7. Нажмите "Create repository"

### 2.4 Загрузка кода на GitHub

```bash
# Переименуйте ветку в main (если нужно)
git branch -M main

# Загрузите код на GitHub
git push -u origin main
```

**Если возникнет ошибка аутентификации:**
- Используйте Personal Access Token вместо пароля
- Или настройте SSH ключи

---

## Шаг 3: Деплой на Vercel

### 3.1 Регистрация на Vercel

1. Перейдите на https://vercel.com
2. Нажмите "Sign Up"
3. Выберите "Continue with GitHub"
4. Авторизуйтесь через GitHub

### 3.2 Импорт проекта

1. В Dashboard Vercel нажмите **"Add New Project"**
2. Выберите репозиторий `mariiafilipets-collab/vusdt-token`
3. Нажмите **"Import"**

### 3.3 Настройка проекта

**Важные настройки:**

1. **Framework Preset:**
   - Выберите: **"Create React App"**

2. **Root Directory:**
   - Нажмите "Edit"
   - Укажите: `frontend`
   - Это важно! Vercel должен знать, где находится React приложение

3. **Build Command:**
   - Оставьте: `npm run build` (или `cd frontend && npm run build`)

4. **Output Directory:**
   - Укажите: `build`
   - Или оставьте по умолчанию, если Root Directory = `frontend`

5. **Install Command:**
   - Оставьте: `npm install` (или `cd frontend && npm install`)

### 3.4 Настройка переменных окружения

**Перед деплоем добавьте переменные окружения:**

1. В настройках проекта найдите **"Environment Variables"**
2. Добавьте следующие переменные:

```
REACT_APP_VUSDT_ADDRESS=0x... (адрес на testnet или mainnet)
REACT_APP_YIELD_DISTRIBUTOR_ADDRESS=0x...
REACT_APP_PURCHASE_CONTRACT_ADDRESS=0x...
REACT_APP_CONVERSION_CONTRACT_ADDRESS=0x...
REACT_APP_USDT_TOKEN_ADDRESS=0x...
```

**Для каждого окружения:**
- Production
- Preview
- Development

**Важно:** 
- Используйте адреса контрактов на **BNB Chain Mainnet** для production
- Или **BSC Testnet** для тестирования

### 3.5 Деплой

1. Нажмите **"Deploy"**
2. Дождитесь завершения сборки (обычно 2-5 минут)
3. После успешного деплоя вы получите URL:
   - `https://vusdt-token.vercel.app` (или похожий)

### 3.6 Проверка деплоя

1. Откройте URL проекта
2. Проверьте:
   - ✅ Сайт загружается
   - ✅ Подключение кошелька работает
   - ✅ Контракты подключаются
   - ✅ Все функции работают

---

## Шаг 4: Настройка домена (опционально)

### 4.1 Добавление кастомного домена

1. В настройках проекта → **"Domains"**
2. Добавьте ваш домен (например, `vusdt.com`)
3. Следуйте инструкциям Vercel для настройки DNS

---

## Шаг 5: Автоматический деплой

### 5.1 Настройка автоматического деплоя

**Vercel автоматически:**
- ✅ Деплоит при каждом push в `main` ветку
- ✅ Создает preview для pull requests
- ✅ Обновляет production при merge

**Для обновления сайта:**
```bash
# Внесите изменения
git add .
git commit -m "Update: описание изменений"
git push origin main

# Vercel автоматически задеплоит обновления
```

---

## 🐛 Решение проблем

### Проблема 1: Ошибка сборки

**Ошибка:** "Build failed"

**Решение:**
1. Проверьте логи сборки в Vercel
2. Убедитесь, что `Root Directory` = `frontend`
3. Проверьте, что все зависимости в `package.json`
4. Убедитесь, что `npm run build` работает локально

### Проблема 2: Переменные окружения не работают

**Ошибка:** Переменные `undefined`

**Решение:**
1. Убедитесь, что переменные добавлены для всех окружений
2. Переменные должны начинаться с `REACT_APP_`
3. После добавления переменных - передеплойте проект

### Проблема 3: Контракты не подключаются

**Ошибка:** "Contract not found"

**Решение:**
1. Проверьте адреса контрактов в переменных окружения
2. Убедитесь, что используете правильную сеть (Mainnet/Testnet)
3. Проверьте, что контракты задеплоены

### Проблема 4: Git push не работает

**Ошибка:** "Authentication failed"

**Решение:**
1. Используйте Personal Access Token:
   - GitHub → Settings → Developer settings → Personal access tokens
   - Создайте токен с правами `repo`
   - Используйте токен вместо пароля

2. Или настройте SSH:
   ```bash
   # Генерируйте SSH ключ
   ssh-keygen -t ed25519 -C "your_email@example.com"
   
   # Добавьте ключ в GitHub
   # Settings → SSH and GPG keys → New SSH key
   
   # Измените remote на SSH
   git remote set-url origin git@github.com:mariiafilipets-collab/vusdt-token.git
   ```

---

## 📋 Чеклист

- [ ] Проект закоммичен локально
- [ ] Репозиторий создан на GitHub
- [ ] Remote добавлен и код загружен
- [ ] Vercel аккаунт создан
- [ ] Проект импортирован в Vercel
- [ ] Root Directory = `frontend`
- [ ] Переменные окружения добавлены
- [ ] Деплой успешен
- [ ] Сайт работает и доступен

---

## 🎯 Быстрая команда для деплоя

```bash
# 1. Коммит и пуш
git add .
git commit -m "Deploy to production"
git push origin main

# 2. Vercel автоматически задеплоит
# Проверьте статус на https://vercel.com/dashboard
```

---

**Готово! Ваш сайт теперь доступен на Vercel! 🚀**

