# Инструкция по настройке проекта VUSDT

## Шаг 1: Установка зависимостей

Все зависимости уже установлены:
- ✅ Backend зависимости (Hardhat, OpenZeppelin и т.д.)
- ✅ Frontend зависимости (React, ethers.js и т.д.)

## Шаг 2: Настройка переменных окружения

### Backend (.env в корне проекта)

1. Скопируйте `.env.example` в `.env`:
```bash
copy .env.example .env
```

2. Отредактируйте `.env` и укажите:
   - `PRIVATE_KEY` - ваш приватный ключ для развертывания (без 0x)
   - `BSCSCAN_API_KEY` или `ETHERSCAN_API_KEY` - API ключ для верификации контрактов (опционально)

   **Как получить API ключ (Etherscan API V2):**
   
   📌 **Важно:** Etherscan API V2 теперь унифицирован для всех EVM-совместимых цепей (Ethereum, BSC, Base, Arbitrum и более 60 других). Один API ключ работает для всех цепей!
   
   1. Перейдите на https://docs.etherscan.io/ или https://bscscan.com/
   2. Зарегистрируйтесь или войдите в аккаунт
   3. Перейдите в раздел API Keys:
      - Для Etherscan: https://etherscan.io/myapikey
      - Для BSCScan: https://bscscan.com/myapikey
   4. Создайте новый API ключ (Etherscan API V2)
   5. Скопируйте ключ в файл `.env` как `BSCSCAN_API_KEY=ваш_ключ`
   
   ✅ **Преимущества Etherscan API V2:**
   - Один ключ для всех EVM-совместимых цепей
   - Работает с BSC, Ethereum, Base, Arbitrum и другими
   - Бесплатный план: 5 запросов в секунду
   
   ⚠️ **Важно:** API ключ опционален и нужен только для автоматической верификации контрактов. Без него вы все равно можете развернуть контракты, но верификацию придется делать вручную через веб-интерфейс.

### Frontend (frontend/.env)

1. После развертывания контрактов создайте `frontend/.env`:
```bash
cd frontend
copy .env.example .env
```

2. Укажите адреса развернутых контрактов:
   - `REACT_APP_VUSDT_ADDRESS` - адрес контракта VUSDT
   - `REACT_APP_YIELD_DISTRIBUTOR_ADDRESS` - адрес контракта YieldDistributor

## Шаг 3: Развертывание контрактов

### На BSC Testnet

```bash
npm run deploy:testnet
```

Сохраните адреса контрактов из вывода.

### На BSC Mainnet

```bash
npm run deploy:mainnet
```

## Шаг 4: Настройка фронтенда

После развертывания обновите `frontend/.env` с адресами контрактов.

## Шаг 5: Запуск фронтенда

```bash
cd frontend
npm start
```

Приложение откроется на http://localhost:3000

## Шаг 6: Начисление доходности

Каждую пятницу в 00:00 UTC вызывайте:

```bash
YIELD_DISTRIBUTOR_ADDRESS=0x... npm run distribute -- --network bscMainnet
```

Или используйте админ-панель в веб-интерфейсе.

## Проверка установки

Запустите тесты:
```bash
npm test
```

Все тесты должны пройти успешно.

## Важные замечания

⚠️ **Безопасность:**
- Никогда не коммитьте файл `.env` в git
- Храните приватные ключи в безопасности
- Тестируйте на testnet перед mainnet

📝 **Примечания:**
- Контракты используют OpenZeppelin библиотеки
- Поддерживается compound процент (12% APY)
- Начисление происходит каждую пятницу в 00:00 UTC

