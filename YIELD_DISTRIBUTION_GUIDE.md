# 📊 Руководство по дистрибуции процентов VUSDT

## 🎯 Обзор процесса

Дистрибуция процентов — это автоматическое начисление VUSDT токенов всем зарегистрированным держателям на основе их баланса и текущей процентной ставки.

---

## ⚙️ Как это работает

### 1. **Механизм начисления**

- **Базовая ставка**: 0.22% в неделю (настраивается в админ панели)
- **Годовая ставка**: ~12% APY (0.22% × 52 недели)
- **Формула расчета**: `yield = balance × weeklyYieldRateBps / 10000`
- **Пример**: При балансе 1000 VUSDT и ставке 0.22% = 2.2 VUSDT в неделю

### 2. **Процесс дистрибуции**

Когда вызывается `distributeYield()`:

1. ✅ Проверяется, прошла ли неделя с последней дистрибуции
2. ✅ Проверяется, не приостановлена ли дистрибуция
3. ✅ Проходится по всем зарегистрированным держателям
4. ✅ Для каждого держателя:
   - Проверяется текущий баланс VUSDT
   - Рассчитывается процент: `balance × weeklyYieldRateBps / 10000`
   - Минтится (создается) новое количество VUSDT на адрес держателя
5. ✅ Обновляется время последней дистрибуции
6. ✅ Эмитируется событие `YieldDistributed` с результатами

### 3. **Регистрация держателей**

Держатель должен быть зарегистрирован, чтобы получать проценты:

- **Автоматически**: При получении токенов (если баланс > 0)
- **Вручную**: Через кнопку "Register for Yield" на сайте
- **Через админ панель**: Администратор может зарегистрировать адреса

---

## 📅 Когда производить дистрибуцию

### Рекомендуемое время

- **Идеально**: Каждую пятницу в 00:00 UTC
- **Минимум**: Раз в неделю (7 дней с последней дистрибуции)
- **Максимум**: Один раз в неделю (контракт не позволит чаще)

### Проверка готовности

Контракт проверяет:
- ✅ Прошла ли неделя с `lastDistributionTime`
- ✅ Не приостановлена ли дистрибуция (`distributionPaused = false`)

---

## 🚀 Способы запуска дистрибуции

### Способ 1: Через админ панель (самый простой)

1. Откройте сайт: `http://localhost:3000`
2. Подключите кошелек владельца контракта
3. Перейдите в раздел **Admin Panel**
4. Найдите секцию **"Yield Distribution"**
5. Нажмите кнопку **"Distribute Yield"**
6. Подтвердите транзакцию в MetaMask
7. Дождитесь подтверждения транзакции

**Преимущества:**
- ✅ Простой интерфейс
- ✅ Видны все статистики
- ✅ Не требует настройки скриптов

---

### Способ 2: Через скрипт Hardhat (для автоматизации)

#### Настройка

1. Откройте файл `.env` в корне проекта
2. Добавьте адрес контракта:
   ```env
   YIELD_DISTRIBUTOR_ADDRESS=0x0a9215946Aff1becF11A71D199aeDfe741CDBEfb
   ```

#### Запуск вручную

```bash
# Для BSC Testnet
npm run distribute -- --network bscTestnet

# Или напрямую через Hardhat
npx hardhat run scripts/distribute-yield.js --network bscTestnet
```

#### Автоматизация через Cron (Linux/Mac)

Создайте файл `crontab` для автоматического запуска каждую пятницу:

```bash
# Редактировать crontab
crontab -e

# Добавить строку (каждую пятницу в 00:00 UTC)
0 0 * * 5 cd /path/to/VUSDT && YIELD_DISTRIBUTOR_ADDRESS=0x0a9215946Aff1becF11A71D199aeDfe741CDBEfb npm run distribute -- --network bscTestnet >> /var/log/vusdt-distribution.log 2>&1
```

#### Автоматизация через Task Scheduler (Windows)

1. Откройте **Task Scheduler** (Планировщик заданий)
2. Создайте новое задание:
   - **Триггер**: Еженедельно, пятница, 00:00 UTC
   - **Действие**: Запустить программу
   - **Программа**: `cmd.exe`
   - **Аргументы**: `/c cd /d D:\VUSDT && set YIELD_DISTRIBUTOR_ADDRESS=0x0a9215946Aff1becF11A71D199aeDfe741CDBEfb && npm run distribute -- --network bscTestnet`

---

### Способ 3: Через Web3 скрипт (Node.js)

Создайте файл `scripts/distribute-web3.js`:

```javascript
const { ethers } = require("ethers");

async function distributeYield() {
  // Подключение к BSC Testnet
  const provider = new ethers.JsonRpcProvider("https://data-seed-prebsc-1-s1.binance.org:8545/");
  
  // Ваш приватный ключ (храните в безопасности!)
  const wallet = new ethers.Wallet(process.env.PRIVATE_KEY, provider);
  
  // Адрес контракта YieldDistributor
  const yieldDistributorAddress = "0x0a9215946Aff1becF11A71D199aeDfe741CDBEfb";
  
  // ABI контракта (только нужные функции)
  const abi = [
    "function distributeYield() external",
    "function canDistribute() external view returns (bool)",
    "function distributionPaused() external view returns (bool)"
  ];
  
  const contract = new ethers.Contract(yieldDistributorAddress, abi, wallet);
  
  // Проверка готовности
  const canDistribute = await contract.canDistribute();
  const isPaused = await contract.distributionPaused();
  
  if (isPaused) {
    console.error("❌ Дистрибуция приостановлена");
    return;
  }
  
  if (!canDistribute) {
    console.error("❌ Еще рано для дистрибуции");
    return;
  }
  
  // Запуск дистрибуции
  console.log("🚀 Запуск дистрибуции...");
  const tx = await contract.distributeYield();
  console.log("📝 Транзакция отправлена:", tx.hash);
  
  const receipt = await tx.wait();
  console.log("✅ Дистрибуция завершена! Блок:", receipt.blockNumber);
}

distributeYield().catch(console.error);
```

Запуск:
```bash
PRIVATE_KEY=your_private_key node scripts/distribute-web3.js
```

---

## 📊 Проверка статуса дистрибуции

### Через админ панель

1. Откройте **Admin Panel**
2. В секции **"Distribution Status"** вы увидите:
   - Текущую ставку (Weekly Yield Rate)
   - Статус (Paused/Active)
   - Время последней дистрибуции
   - Количество держателей

### Через консоль Hardhat

```javascript
// В консоли Hardhat
const yieldDistributor = await ethers.getContractAt("YieldDistributor", "0x0a9215946Aff1becF11A71D199aeDfe741CDBEfb");

// Проверка статуса
const canDistribute = await yieldDistributor.canDistribute();
const isPaused = await yieldDistributor.distributionPaused();
const lastDistribution = await yieldDistributor.lastDistributionTime();
const weeklyRate = await yieldDistributor.weeklyYieldRateBps();
const holderCount = await yieldDistributor.getHolderCount();

console.log("Можно дистрибутировать:", canDistribute);
console.log("Приостановлено:", isPaused);
console.log("Последняя дистрибуция:", new Date(Number(lastDistribution) * 1000));
console.log("Недельная ставка:", Number(weeklyRate) / 100, "%");
console.log("Количество держателей:", Number(holderCount));
```

### Через BSCScan

1. Откройте контракт на BSCScan: `https://testnet.bscscan.com/address/0x0a9215946Aff1becF11A71D199aeDfe741CDBEfb`
2. Перейдите в раздел **"Read Contract"**
3. Проверьте:
   - `canDistribute()` — можно ли дистрибутировать
   - `lastDistributionTime()` — время последней дистрибуции
   - `weeklyYieldRateBps()` — текущая ставка
   - `getHolderCount()` — количество держателей

---

## 🔍 Что происходит внутри контракта

### Функция `distributeYield()`

```solidity
function distributeYield() external nonReentrant {
    // 1. Проверка условий
    require(canDistribute(), "Cannot distribute yet");
    
    // 2. Обновление времени
    lastDistributionTime = block.timestamp;
    
    // 3. Обработка всех держателей
    for (uint256 i = 0; i < holders.length; i++) {
        address holder = holders[i];
        uint256 balance = vusdtToken.balanceOf(holder);
        
        if (balance == 0) {
            removeHolderIfNeeded(holder);
            continue;
        }
        
        // 4. Расчет и минт процентов
        uint256 yieldAmount = calculateYield(balance);
        if (yieldAmount > 0) {
            vusdtToken.mint(holder, yieldAmount);
            totalDistributed += yieldAmount;
        }
    }
    
    // 5. Событие
    emit YieldDistributed(totalDistributed, holdersToProcess, block.timestamp);
}
```

### Расчет процентов

```solidity
function calculateYield(uint256 balance) public view returns (uint256) {
    if (balance == 0) return 0;
    // balance * weeklyYieldRateBps / 10000
    return (balance * weeklyYieldRateBps) / 10000;
}
```

**Примеры:**
- Баланс: 1000 VUSDT, Ставка: 0.22% → 2.2 VUSDT
- Баланс: 10000 VUSDT, Ставка: 0.22% → 22 VUSDT
- Баланс: 100000 VUSDT, Ставка: 0.22% → 220 VUSDT

---

## ⚠️ Важные моменты

### 1. **Газовые расходы**

- Дистрибуция требует газа (BNB) для выполнения транзакции
- Чем больше держателей, тем больше газа потребуется
- Убедитесь, что у кошелька достаточно BNB

### 2. **Время выполнения**

- Для 100 держателей: ~30-60 секунд
- Для 1000 держателей: ~5-10 минут
- Для 10000+ держателей: может потребоваться оптимизация

### 3. **Ограничения**

- ✅ Минимум 7 дней между дистрибуциями
- ✅ Дистрибуция должна быть не приостановлена
- ✅ Держатели должны быть зарегистрированы
- ✅ Держатели должны иметь баланс > 0

### 4. **Безопасность**

- ⚠️ Приватный ключ должен храниться в безопасности
- ⚠️ Не коммитьте `.env` файлы в Git
- ⚠️ Используйте отдельный кошелек для автоматизации
- ⚠️ Регулярно проверяйте баланс кошелька

---

## 🛠️ Управление дистрибуцией

### Приостановка дистрибуции

**Через админ панель:**
1. Admin Panel → "Pause Distribution"
2. Подтвердите транзакцию

**Через скрипт:**
```javascript
await yieldDistributor.pauseDistribution();
```

### Возобновление дистрибуции

**Через админ панель:**
1. Admin Panel → "Resume Distribution"
2. Подтвердите транзакцию

**Через скрипт:**
```javascript
await yieldDistributor.resumeDistribution();
```

### Изменение ставки

**Через админ панель:**
1. Admin Panel → "Weekly Yield Rate Settings"
2. Введите новую ставку (0-10%)
3. Нажмите "Update Yield Rate"

**Через скрипт:**
```javascript
// Ставка в basis points (220 = 2.2%)
await yieldDistributor.setWeeklyYieldRate(220);
```

---

## 📈 Мониторинг и логирование

### Просмотр событий

Все дистрибуции записываются в событие `YieldDistributed`:

```javascript
// Просмотр последних событий
const filter = yieldDistributor.filters.YieldDistributed();
const events = await yieldDistributor.queryFilter(filter, -1000); // Последние 1000 блоков

events.forEach(event => {
    console.log("Total distributed:", ethers.formatEther(event.args.totalDistributed));
    console.log("Holders:", event.args.holdersCount.toString());
    console.log("Time:", new Date(Number(event.args.timestamp) * 1000));
});
```

### Логирование результатов

Скрипт `distribute-yield.js` автоматически выводит:
- Количество обработанных держателей
- Общее количество распределенных токенов
- Время дистрибуции
- Новый total supply

---

## 🎯 Рекомендации

1. **Регулярность**: Запускайте дистрибуцию каждую пятницу в 00:00 UTC
2. **Мониторинг**: Проверяйте статус перед каждой дистрибуцией
3. **Резерв**: Держите запас BNB для газовых расходов
4. **Автоматизация**: Используйте cron/Task Scheduler для автоматического запуска
5. **Уведомления**: Настройте уведомления об успешных/неуспешных дистрибуциях

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи транзакции на BSCScan
2. Убедитесь, что прошла неделя с последней дистрибуции
3. Проверьте, что дистрибуция не приостановлена
4. Убедитесь, что есть зарегистрированные держатели
5. Проверьте баланс BNB для газа

---

## 📝 Пример полного процесса

```bash
# 1. Проверка статуса
npx hardhat console --network bscTestnet
> const yd = await ethers.getContractAt("YieldDistributor", "0x0a9215946Aff1becF11A71D199aeDfe741CDBEfb");
> await yd.canDistribute()
true

# 2. Запуск дистрибуции
YIELD_DISTRIBUTOR_ADDRESS=0x0a9215946Aff1becF11A71D199aeDfe741CDBEfb npm run distribute -- --network bscTestnet

# 3. Проверка результатов
> const events = await yd.queryFilter(yd.filters.YieldDistributed(), -10);
> events[events.length - 1].args
```

---

**Готово! Теперь вы знаете, как производить дистрибуцию процентов.** 🎉

